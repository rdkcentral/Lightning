Canvas2d render implementatie:

TODO:

Save texture coords during addQuads as well.
Because they are also temporarily saved.

Colorizing images.

Fallback for corner gradients.

Patterns (repeating textures?): texture params.

Set image smoothing based on texture (if gl.NEAREST).

