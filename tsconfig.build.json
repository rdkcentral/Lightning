/*
 * Production build tsconfig
 */
{
  "extends": "./tsconfig.json",
  "include": [
    "./index.ts", "./src/**/*.ts", "./src/**/*.mts"
  ],
  "compilerOptions": {
    "outDir": "./dist",
    // Make sure we emit files from this tsconfig
    "noEmit": false,
  }
}