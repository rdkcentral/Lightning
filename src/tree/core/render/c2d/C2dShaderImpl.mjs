export default class C2dShaderImpl {

    constructor(shader) {
        this._shader = shader;
    }

    beforeDraw(operation) {
    }

    draw(operation) {
    }

    afterDraw(operation) {
    }

}
